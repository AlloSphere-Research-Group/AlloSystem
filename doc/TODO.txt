This is a master to do list for AlloCore.

________________________________________________________________________________

Source Code
________________________________________________________________________________
graphics/
- move FBO out of pending/
- Slab and Surface, document or die!
- add nurbs abstraction

io/
- TCP/IP support

protocol/
- move OpenCL into its own module

math/
- move Matrix4 to spatial/? Most operations are related to space...

sound/
- switch to jack for audio i/o

________________________________________________________________________________

Unit Testing
________________________________________________________________________________

General
--------------------------------------------------------------------------------
- Ensure that all objects default construct and destruct without run-time 
errors. This will also be handy for "touching" all templated code to check for 
compilation errors.

FIXME
--------------------------------------------------------------------------------
protocol/OSC
- on 64-bit platforms, ending a bundle with a message results in a seg fault


Specific
--------------------------------------------------------------------------------
graphics/
Asset
	- load scene file (succeeds if no crash)
	- check that structure in Scene matches file
Font
	- load a font file (succeeds if no crash)	
	- check that metrics make sense
GPUObject
	- test signaling from GPUContext
Graphics
	- immediate mode emulation
Image
	- load image (succeeds if no crash)
	- ensure array data makes sense
Isosurface
	- run on small volume
	- index conversion calculations
Mesh
	- add elements to various buffers and check sizes of buffers
	- check transformation methods to see that they do as advertised
Shader
	- test default (no-op) vert/frag shaders (error log should be empty)
Slab
	- ???
Stereographic
	- ???
Surface
	- ???
Texture
	- ???


io/
AudioIO
	- check that -1 opens all channels
	- what happens when we try opening 0 channels?
	- what happens when we try opening 1,000,000 channels?
	- ensure callback gets called

File
	- tests for SearchPaths and FilePath

Window
	- opening and closing (check that onCreate, onDestroy called properly)
	- fullscreen (check correctness of arguments passed to onResize)
	- check that various getters correlate


math/
	- Mat/Vec multiplication
	- Matrix4 operations (apply then do inverse)
	- Quat coordinate conversions (apply then do inverse)


sound/

________________________________________________________________________________

Examples and Project Ideas
________________________________________________________________________________

gui/
	- modify GLV presets.cpp example to load presets

guiAudio/
	- drum machine with step sequencer
	- bass-line synth with step sequencer
		- extra credit: save and load preset patterns
	- pulsar generator a la Roads

guiVisual/
	- Lumia synth a la Wilfred's Clavilux
	- supershape synth
	- shader designer
	- GUI designer
	- generalized 3D volume renderer
	- simple stereographic testing tool

guiAudiovisual/
	- oscillograph synth a la Laposky's Oscillons
	- time/frequency scopes of line-input
		- extra credit: monitor n channels simultaneously

audio/
	- basic synthesis (sine, modulated sine); check Gamma examples/
	- Note list parser (Csound-style)

graphics/
	- Shaders: this could turn into a GLSL tutorial of sorts
		- combined fog and lighting
		- simple vertex transforms
		- simple geometry shader demo
		- more curvey shape for Phong lighting demo
	- Texture mapping
	- Simple particle system

world/
	- OSC-navigable static 3D scene

